# {{WORKSHOP}} — todo

> Last updated: {{DATE}}

## [SETUP] The decisions the installer did not take

> status: active

This piece of work is the accompanying note, not content. It is written to be **deleted** —
when its last item closes, the whole section leaves the todo.

- ❓ Where `_workspace/` is pushed. The repository exists with one commit and no remote, which
  is deliberate: the host is an owner's decision. ⚠ Until then nothing here is backed up — a
  commit that is never pushed protects nothing (`method/git.md`)
- ⚠ `lint.toml` ships the method's **default** size thresholds, not a measurement of this
  workshop. Nothing is due now. Replace them with measured ones the day this workshop has content
  worth measuring, and write the measurement beside each — the formula is in
  `method/tracking.md` § *Sizes*
- ⬜ Declare any **further** repository in `lint.toml` § `[workshop]` as you create it, so the
  checks cover it. Those the installer was given are already listed
- ⬜ Write the first atlas entry. Until it says something, resuming on solid ground means
  reading the history — which is the thing an atlas exists to make unnecessary
