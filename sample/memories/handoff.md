# Handoff

> Volatile context only: what is in progress, blocked, or intended next. The consolidated state
> is in `atlas.md`, the open in `todo.md`, the factual record in the `git log`. Keep the recent
> sessions, archive the rest. An illustration; the repository readme says what it shows.
> Last updated: 2026-06-14

## Session of 2026-06-14

Rounding, all afternoon, and nothing shipped — deliberately.

**Done**: `_planning/rounding.md` written. The two candidates are laid out with the cases each
gets wrong, and the comparison turned up a third that nobody had considered and that is worse
than both. `[ROUNDING]` now cites that document instead of carrying the argument.

**In progress or blocked**: `[ACCOUNTS]` waits on the arbitration in `[OPEN]` and has waited a
week; it is one branch of code away from done, which is exactly why it is worth naming rather
than quietly leaving.

**Next**: the rounding decision, then `[ACCOUNTS]` if the arbitration has landed.

⚠ Reading the export narrative for an unrelated reason turned up something: `ledger.web` still
formats one amount for display without going through the shared helper. Noted, not chased — it
is not what this session was about.

## Session of 2026-06-11

Export delivered and the tag closed; `done/EXPORT.md` holds the account of it, `atlas.md` keeps
the result in four lines.

The reporting work has moved to its own workshop, which now owns its framing.
